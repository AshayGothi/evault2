{
  "name": "evault",
  "version": "1.0.0",
  "description": "eVault - Legal document storage on blockchain",
  "main": "server/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/index.js",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run install-client && npm run install-server && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "install": "^0.13.0",
    "ipfs-http-client": "^57.0.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.2",
    "multer": "^1.4.5-lts.2",
    "nodemailer": "^6.10.1",
    "web3": "^4.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
